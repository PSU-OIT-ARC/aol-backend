DEBUG: True
DEBUG_PROPOGATE_EXCEPTIONS: True
TEMPLATE_DEBUG: True
ALLOWED_HOSTS:
  - jarmusch.lan.ninjawirel.us
INTERNAL_IPS:
  - localhost
  - '172.20.0.1'
CSRF_COOKIE_SECURE: False
CORS_ORIGIN_ALLOW_ALL: True
MEDIA_ROOT: /webapps/aol/media
STATIC_ROOT: /webapps/aol/static
SENDFILE_BACKEND: django_sendfile.backends.development
SENDFILE_ROOT: /webapps/aol/media
DATABASES:
  default:
    HOST: postgres
    USER: aol
    NAME: aol
CELERY_BROKER_URL: 'pyamqp://guest:guest@rabbitmq//'
